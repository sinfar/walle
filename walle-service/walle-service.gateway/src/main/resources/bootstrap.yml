server:
  port: 8090

spring:
  application:
    name: gateway
  main:
    allow-bean-definition-overriding: true


  cloud:
    nacos:
      config:
        shared-configs: ${spring.application.name}.yaml,common-redis.yaml
